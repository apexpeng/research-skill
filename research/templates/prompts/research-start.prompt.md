# /research 启动提示词

```text
/research 课题：[填写你的课题]
目标：[填写本轮目标，例如“先出中文主线和大纲”]
已有材料：[结果/图/草稿/笔记]
约束：[目标期刊、时间限制、结论边界]
```

推荐补充：

```text
请先判断当前阶段，再按Coordinator -> Reviewer -> Executor -> Supervisor的接力推进；同一角色不能同时担任执行和最终裁判。默认：MAX_ROUNDS=4，POSITIVE_THRESHOLD=6/10，REVIEWER_MODEL=会话当前默认模型（Codex）。
```
